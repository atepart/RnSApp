# Реестр дефектов

## Открытые дефекты

### BUG-20260722-2 — Общий unittest-процесс не завершается после Qt-тестов

- Дата: 2026-07-22
- Статус: Подтверждён
- Серьёзность: Низкая
- Компонент: build
- Связанная работа: нет
- Окружение: macOS, Python 3.11, PySide6 6.8.2.1,
  `QT_QPA_PLATFORM=offscreen`
- Предусловия: запуск всех пяти модулей `tests/` одним процессом unittest.
- Шаги воспроизведения:
  1. Запустить `python -m unittest` через обязательные `rtk`, `uv` и offscreen Qt.
  2. Дождаться фактического выполнения тестов.
- Фактический результат: процесс остаётся в фоне без итогового кода выхода;
  повторные запуски накапливают процессы Python/uv/rtk.
- Ожидаемый результат: после выполнения 13 тестов процесс печатает итог и
  завершается с кодом 0.
- Доказательства: 2026-07-22 обнаружены три зависшие цепочки этого проекта;
  после их точечного завершения все пять модулей отдельно прошли с кодом 0
  (3 + 2 + 1 + 6 + 1 тест).
- Регрессионный тест: ещё не добавлен

## Исправленные дефекты

### BUG-20260722-1 — Fit-серия XLSX не имеет кэшированных точек

- Дата: 2026-07-22
- Дата исправления: 2026-07-22
- Статус: Исправлен
- Серьёзность: Средняя
- Компонент: infrastructure
- Связанная работа: WORK-20260722-1
- Окружение: macOS, Python 3.11, openpyxl, LibreOffice 26.8 и OOXML-рендерер
- Предусловия: экспортировать рассчитанную ячейку в XLSX и открыть файл в
  просмотрщике, который не вычисляет вложенные формулы для кэша графика.
- Шаги воспроизведения:
  1. Экспортировать XLSX с weighted fit.
  2. Отрендерить лист без полного пересчёта Microsoft Excel.
- Фактический результат: формулы slope/intercept корректны, но у fit-серии
  пустой числовой кэш; отдельные просмотрщики показывают линию в неверной
  позиции до пересчёта книги.
- Ожидаемый результат: при первом открытии серия имеет две согласованные точки,
  а после изменения данных продолжает обновляться по формулам листа.
- Доказательства: исходный `chart1.xml` содержал `ptCount=0` для X/Y fit-серии;
  контрольный рендер сохранён в `/tmp/rns-xlsx-style/outputs/weighted_fit_style_20260722`.
- Регрессионный тест: `tests/test_xlsx_weighted_fit.py`
- Проверки исправления: XLSX-регрессионный тест и полный unittest-набор пройдены;
  нативный Cocoa-сценарий повторно экспортировал книгу. Исходный OOXML отрендерен
  с корректной fit-линией, LibreOffice пересчитал slope/intercept до
  `0,0499999511 / 0,1000007044` без ошибок ячеек.

### BUG-20260720-2 — Пустой Rn искажает weighted fit в XLSX

- Дата: 2026-07-20
- Дата исправления: 2026-07-22
- Статус: Исправлен
- Серьёзность: Высокая
- Компонент: infrastructure
- Связанная работа: WORK-20260720-5
- Окружение: macOS, Python 3.11, openpyxl, LibreOffice 26.8; экспортированный
  формат XLSX предназначен для Microsoft Excel 16.93
- Предусловия: выбранная строка содержит положительный диаметр, но пустой или
  некорректный `Rn`; остальные строки достаточны для линейного fit.
- Шаги воспроизведения:
  1. Экспортировать ячейку с такой строкой в XLSX.
  2. Пересчитать формулы книги.
- Фактический результат: в `J` получается пустое значение, но в `K` остаётся
  вес `1/D`; `SUMPRODUCT` учитывает строку как точку с `Y=0`. На контрольных
  данных наклон/пересечение меняются с ожидаемых `0,05 / 0,10` на
  `0,0282608 / 0,0930442`.
- Ожидаемый результат: строка без числового `1/√Rₙ` полностью исключается из
  fit, как это происходит в приложении.
- Доказательства: исправленный файл
  `/tmp/rns-weighted-repo-final/rns-weighted-excel-missing-rn-diagnostic.xlsx`
  после пересчёта возвращает наклон/пересечение
  `0,0500000392 / 0,1000007326` и не содержит ошибок формул.
- Регрессионный тест: `tests/test_xlsx_weighted_fit.py`
- Проверки исправления: 7 релевантных тестов, полный unittest-набор и нативный
  Cocoa UI-сценарий пройдены; XLSX пересчитан LibreOffice 26.8 без ремонта.

### BUG-20260720-1 — Небезопасное закрытие всех top-level окон

- Дата: 2026-07-20
- Статус: Исправлен
- Серьёзность: Низкая
- Компонент: ui
- Связанная работа: WORK-20260720-2
- Окружение: macOS, Python 3.11, PySide6 6.8.2.1, Qt integration-тест
- Предусловия: вместе с главным окном существует другое top-level Qt-окно
- Шаги воспроизведения:
  1. Открыть RnSApp и дополнительное top-level окно графика.
  2. Закрыть главное окно RnSApp.
- Фактический результат: `closeEvent` повторно вызывал `close()` для самого
  главного окна и без защиты закрывал остальные окна; повреждённое окно графика
  могло вызвать `AttributeError` при закрытии.
- Ожидаемый результат: главное окно не закрывает себя рекурсивно, а ошибки при
  закрытии вспомогательных окон не прерывают завершение приложения.
- Доказательства: воспроизведено полным Qt test suite до исправления.
- Регрессионный тест: `tests/test_ui_integration.py`
- Проверки исправления: 13 тестов и реальный macOS Cocoa-сценарий пройдены.
